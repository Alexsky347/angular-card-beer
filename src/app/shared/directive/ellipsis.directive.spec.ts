import { EllipsisDirective } from './ellipsis.directive';

describe('ElipsisDirective', () => {
  it('should create an instance', () => {
    const directive = new EllipsisDirective();
    expect(directive).toBeTruthy();
  });
});
